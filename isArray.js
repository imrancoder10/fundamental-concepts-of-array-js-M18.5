const friends = ['Amena', 'Kamena', 'Jamena', 'Pabena', 'Nibena'];
const nums = [];
const food = 'Ros o gol l a';
const age = 15;
const inviteMe = true;

console.log(Array.isArray(friends)); // true
console.log(Array.isArray(nums)); // true
console.log(Array.isArray(food)); // false
console.log(Array.isArray(age)); // false
console.log(Array.isArray(inviteMe)); // false
